# The featurizer application

## How to compile

  - update bioshell v.4 submodule:
  ```
  git submodule init
  git submodule update --rebase --remote
  ```
  
  - compile the app:
  ```
  cd featurizer
  cargo build --release
  ```